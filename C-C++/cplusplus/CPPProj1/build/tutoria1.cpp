#include <iostream> 	// header file, a file our program requires to use 

using namespace std; 	// tabs and spaces in C++ does not have any impact on unlike python, 
						// std stands for standard library

int main()				// beginning of main function, Things we want the computer to do
{
	cout << "Hello World!" << endl; // the output stream object and endline = endl
	return 0;						// functions always return an answer i.e. after say a calcuation
}									// Return 0 means that main ran fine and we are good to go
